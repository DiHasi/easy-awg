# LEGACY: configuration for the original single-server app (root Program.cs, tag
# v0.9-standalone). For the fleet setup use .env.control.example / .env.node.example.
WEB_PORT=8080
AWG_PORT=51820
AWG_SUBNET=10.8.0.0/24
AWG_CLIENT_ALLOWED_IPS=0.0.0.0/0, ::/0
AWG_ENDPOINT_HOST=vpn.example.com
AWG_CLIENT_DNS=1.1.1.1, 8.8.8.8
